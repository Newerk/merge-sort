//takes in an array and returns a sorted array, using a recursive merge sort methodology
const mergeSort = (array) => {
    let arrayCopy = [...array];
    console.log(`this is a shallow copy: ${arrayCopy}`);
};

mergeSort([11,2,5,9,43]);